sudo apt-get update
sudo apt-get install -y ca-certificates curl gnupg
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key | sudo gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg
NODE_MAJOR=22
echo "deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_$NODE_MAJOR.x nodistro main" | sudo tee /etc/apt/sources.list.d/nodesource.list
sudo apt-get install nodejs -y
sudo apt install npm
history
npm install --save-dev @nomiclabs/hardhat-ethers ethers@5.7.2  @nomiclabs/hardhat-waffle ethereum-waffle chai
history
npm i @openzeppelin/contracts@4.5.0
npx create-react-app nftmarket
cd nftmarket/
npx hardhat init

# double check dns
vi hardhat.config.js

vi contracts/Hello.sol

# double check dns
vi src/App.js

# from project directory
mkdir scripts       
vi scripts/deploy.js





# set hardhat config file - to your 'dns' every time you restart instance
2) run remote blockchain
3) fire up the console......

/home/ubuntu/hello   npx hardhat node --hostname ec2-35-86-73-162.us-west-2.compute.amazonaws.com
/home/ubuntu/hello npx   hardhat run scripts/deploy.js --network localhost

# test from hardhat console

npx hardhat console --network localhost

> const feePercent= 1
undefined
> const tokenURI = "https://example.com/metadata/1.json";
undefined
> const [deployer, seller, buyer] = await ethers.getSigners();
undefined
> const NFT =  await ethers.getContractFactory("NFT")
undefined
> let price = ethers.parseEther("1")
undefined
> const nft = await NFT.deploy()
undefined
> const Marketplace =  await ethers.getContractFactory("Marketplace")
undefined
> const marketplace = await Marketplace.deploy(feePercent)
undefined
> marketplaceAddress = await marketplace.getAddress();
'0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512'
> marketplaceAddress
'0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512'


> await marketplace.feeAccount()
'0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266'
> deployer.address
'0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266'
> await marketplace.feePercent()
1n
> feePercent
1
> await marketplace.itemCount()
0n
> 0
0
>
> nft.address
undefined
> let nftAddress = await nft.getAddress()
undefined
> nftAddress
'0x5FbDB2315678afecb367f032d93F642f64180aa3'

> await nft.connect(seller).mint(tokenURI)

> await nft.connect(seller).approve(marketplaceAddress, 1);
> await marketplace.connect(seller).makeItem(nftAddress, 1, price)



await nft.connect( seller ).mint("https://example.com/metadta/2.json")

> await nft.connect( seller ).approve(marketplaceAddress, 2)
> await nft.connect( seller ).mint("https://example.com/metadta/2.json")
> await marketplace.connect(seller).makeItem(nftAddress, 1, price)

> const totalPrice = await marketplace.getTotalPrice(1)

> await marketplace.connect(buyer).purchaseItem( 1, { value: totalPrice })
> await marketplace.items(1)
Result(6) [
  1n,
  '0x5FbDB2315678afecb367f032d93F642f64180aa3',
  1n,
  1000000000000000000n,
  '0x70997970C51812dc3A010C7d01b50e0d17dc79C8',
  true
]
> await marketplace.items(2)
Result(6) [
  2n,
  '0x5FbDB2315678afecb367f032d93F642f64180aa3',
  2n,
  1000000000000000000n,
  '0x70997970C51812dc3A010C7d01b50e0d17dc79C8',
  false
]
> await marketplace.getTotalPrice(1)
1010000000000000000n




>
> await marketplace.connect(buyer).purchaseItem(999, { value: ethers.parseEther("2"), } )
Uncaught:
ProviderError: Error: VM Exception while processing transaction: reverted with reason string 'item doesn't exist'
